{
    "enable_json_format": {
        "description": "启用 JSON 响应格式控制",
        "type": "bool",
        "hint": "开启后 AI 强制返回 JSON 格式。社交媒体场景使用，普通聊天建议关闭",
        "default": false
    },
    "max_messages": {
        "description": "AI 最多返回消息条数",
        "type": "int",
        "hint": "超过此数量的消息会被截断，设为 1 则只返回一条",
        "default": 3
    },
    "max_chars_per_message": {
        "description": "每条消息最多字符数",
        "type": "int",
        "hint": "超过此长度的消息会被截断，适用于没有 Markdown 渲染的聊天平台",
        "default": 200
    }
}
