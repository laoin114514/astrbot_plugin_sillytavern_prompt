{
    "default_user_name": {
        "description": "默认用户名 (替换 {{user}} 宏)",
        "type": "string",
        "hint": "在角色扮演中，你的称呼",
        "default": "User"
    },
    "default_persona": {
        "description": "默认用户 Persona 描述 (替换 {{persona}} 宏)",
        "type": "text",
        "hint": "描述你自己的身份、性格、背景等",
        "default": ""
    },
    "default_preset": {
        "description": "默认 Prompt 预设",
        "type": "string",
        "hint": "插件启动时自动加载的预设名称，留空使用内置 default",
        "default": "default"
    },
    "astrbot_persona_for_tools": {
        "description": "AstrBot 人格 (控制 tools/skills/MCP 可用性)",
        "type": "string",
        "hint": "选择 AstrBot 中已配置的人格。system_prompt 由插件完全接管，此人格仅决定 tools/skills/MCP 是否可用",
        "default": "",
        "_special": "select_persona"
    },
    "wrap_sections_with_labels": {
        "description": "为每个 section 添加标签头",
        "type": "bool",
        "hint": "开启后在每段提示词前添加 [标题] 标签，帮助 LLM 理解结构",
        "default": true
    },
    "include_dialogue_examples": {
        "description": "包含对话示例",
        "type": "bool",
        "hint": "将角色卡的 mes_example 注入到 system_prompt 中",
        "default": true
    }
}
