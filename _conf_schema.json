{
    "default_card": {
        "description": "默认角色卡名称",
        "type": "string",
        "hint": "插件启动后自动加载的角色卡。使用 /st_char create <名称> 创建新角色卡",
        "default": "default"
    },
    "wrap_sections_with_labels": {
        "description": "为每个 section 添加标签头",
        "type": "bool",
        "hint": "开启后在每段提示词前添加 [标题] 标签，帮助 LLM 理解结构",
        "default": true
    },
    "include_dialogue_examples": {
        "description": "包含对话示例",
        "type": "bool",
        "hint": "将角色卡的 mes_example 注入到 system_prompt 中",
        "default": true
    }
}
